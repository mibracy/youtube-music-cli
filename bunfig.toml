# bunfig.toml — project-specific Bun settings
# https://bun.sh/docs/runtime/bunfig

[install]
registry = "https://registry.npmjs.org/"
global = false

[test]
coverage = false
parallel = true
watch = false

[run]
compat = "bun"
